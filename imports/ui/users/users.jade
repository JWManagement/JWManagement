template(name="user.search")
    +SearchForm data=data

template(name="user.details")
    +DetailsForm data=data

template(name="user.update")
    +UpdateForm data=data

template(name="user.insert")
    +InsertForm data=data

template(name="user.password.insert")
    +InsertForm data=data

template(name="user.availability.details")
    +DetailsForm data=data

template(name="user.availability.insert")
    +InsertForm data=data

template(name="user.vacation.insert")
    +InsertForm data=data
