const data = {

    // the key (for complex db structures, replace . with _)
    key: 'username',

    // OPTIONAL: the key if the field has to be referenced a different way internally
    linkedKey: 'usernameId',

    // type of the field
    type: 'textbox',

    // OPTIONAL: the icon to be displayed left of the row
    icon: 'group',

    // OPTIONAL: if the row should be readonly
    readonly: true

};
